{
  "name": "@seta/auth",
  "version": "0.1.0",
  "description": "API key hashing + AES-GCM + RBAC + KmsProvider",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "license": "Apache-2.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "private": true,
  "files": [
    "dist"
  ],
  "dependencies": {
    "@node-rs/argon2": "2.0.2",
    "@seta/db": "workspace:*",
    "dotenv": "17.4.2",
    "drizzle-orm": "0.45.2",
    "lru-cache": "11.3.6",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@seta/tsconfig": "workspace:*",
    "@types/node": "^25.7.0",
    "drizzle-kit": "0.31.10",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.6"
  }
}
