#!/usr/bin/env tsx
// TODO: GitHub Actions timing tracker. Spec §18.
// Parses workflow run timing JSON nightly and posts a 7-day rolling chart
// to Slack. Implement once we have a Slack webhook + GH PAT in secrets.
console.error('measure-ci: not yet implemented')
process.exit(1)
