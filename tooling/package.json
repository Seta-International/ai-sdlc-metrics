{
  "name": "@seta/tooling",
  "version": "0.0.0",
  "description": "Repo-wide CLI scripts (migrate, seed, package scaffolding). Not published.",
  "private": true,
  "type": "module",
  "dependencies": {
    "@seta/db": "workspace:*",
    "dotenv": "17.4.2"
  }
}
