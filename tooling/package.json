{
  "name": "@seta/tooling",
  "version": "0.0.0",
  "description": "Repo-wide CLI scripts (migrate, seed, package scaffolding). Not published.",
  "private": true,
  "type": "module",
  "dependencies": {
    "@seta/audit": "workspace:*",
    "@seta/connector-ms365-directory": "workspace:*",
    "@seta/connector-ms365-planner": "workspace:*",
    "@seta/connector-registry": "workspace:*",
    "@seta/db": "workspace:*",
    "@seta/oauth": "workspace:*",
    "dotenv": "17.4.2",
    "postgres": "3.4.9",
    "zod": "4.4.3"
  },
  "scripts": {
    "gen-mock": "tsx scripts/mock-data-generator/src/cli.ts"
  }
}
