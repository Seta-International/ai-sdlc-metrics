// Placeholder — full implementation comes in Task 7
import type { TaskFlat } from '@future/api-client/planner'

export function ActionsCell(_props: { task: TaskFlat; onOpen: (taskId: string) => void }) {
  return null
}
