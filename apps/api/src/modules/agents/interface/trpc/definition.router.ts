import { router } from '../../../../common/trpc/trpc-init'

// TODO: add procedures for agent definitions (CRUD for agent configs, tool bindings, system prompts)
export const definitionRouter = router({})
