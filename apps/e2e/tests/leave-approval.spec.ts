import { test } from '@playwright/test'

test.describe('Leave approval flow', () => {
  test('employee submits leave request and manager approves', async () => {
    // TODO: implement once web-time leave request UI is built
    // (web-time/src/app/page.tsx currently shows "Coming soon.")
    test.todo()
  })
})
