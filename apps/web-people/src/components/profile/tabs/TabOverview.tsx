'use client'

import type { EmployeeProfile } from '../../../lib/types'

interface TabOverviewProps {
  profile: EmployeeProfile
  canEditPersonal: boolean
  canViewSalary: boolean
}

export function TabOverview(_props: TabOverviewProps) {
  return null
}
