{
  "name": "@future/cubejs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cubejs-server",
    "build": "echo \"Cube.js: no build step — deployed as a server (cubejs-server server)\""
  },
  "dependencies": {
    "@cubejs-backend/athena-driver": "^1.6",
    "@cubejs-backend/postgres-driver": "^1.6",
    "@cubejs-backend/server": "^1.6"
  }
}
